import { Alert } from "react-bootstrap";

const Footer = () => {
    return (
        <Alert variant="info" className="text-center mt-4 mb-0 rounded-0">
            Ứng dụng Đặt lịch Khám sức khỏe &copy; 2025. Bản quyền thuộc Trường Đại học Mở TP.HCM.
        </Alert>
    );
};

export default Footer;
